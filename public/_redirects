# Redirect all .html files to non-.html versions
/calculator/*.html /calculator/:splat 301!
/articles/*.html /articles/:splat 301!

# Redirect trailing slashes to non-trailing
/calculator/*/ /calculator/:splat 301!
/articles/*/ /articles/:splat 301!

# Catch-all for any .html in root
/*.html /:splat 301!